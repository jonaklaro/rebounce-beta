using UnityEngine;

public static class PlayerColors
{
    public static Color[] playerColors = new Color[]
    {
        new Color(0.6f, 0.2f, 0.4f), // Red
        new Color(0.2f, 0.4f, 0.8f), // Blue
        new Color(0.2f, 0.8f, 0.3f), // Green
        new Color(0.9f, 0.7f, 0.2f)  // Yellow
    };
}
